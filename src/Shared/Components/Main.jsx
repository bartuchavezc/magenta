import styled from "styled-components";

const Main = styled.main`
  min-height: calc(150vh - 117px);
  display: flex;
  flex-wrap: wrap;
  position: relative;
  overflow: hidden;
`;

export default Main;
